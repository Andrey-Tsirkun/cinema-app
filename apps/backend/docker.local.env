# Used by `npm run dev:all` (dotenv-cli --override) so DB URL matches docker-compose defaults.
# Host ports published by compose: Postgres 5432, Redis 6379.
DATABASE_URL=postgresql://cinema:cinema@127.0.0.1:5432/cinema?schema=public&sslmode=disable
REDIS_URL=redis://127.0.0.1:6379
